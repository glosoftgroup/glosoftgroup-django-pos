/*
    Algorithm:
    (loggedin user id)+(last order id) + (random(1,10))

    Order List Api New Order

    New Order

    GET /api/order/number

    HTTP 200 OK
    Allow: POST, OPTIONS, GET
    Content-Type: application/json
    Vary: Accept
*/

{
    "number": "RET#36"
}

